## WIP
import lark
from .parser import Parser
from .grammar import lvs_grammar

# Compile embedded rule names
# Validate: all rules are defined, all user fns are defined, pattern variable checker order
# Put checker into the place of pattern
